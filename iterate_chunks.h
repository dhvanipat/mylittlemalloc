#ifndef ITERATE_CHUNKS_H
#define ITERATE_CHUNKS_H

#include <stdio.h>
#include <stdlib.h>
#define MEMORY_AVAILABLE 4096 //must change manually if memlength changes

void print_chunk_info(unsigned int*);
void display_all_chunks(unsigned int*);

#endif //ITERATE_CHUNKS_H